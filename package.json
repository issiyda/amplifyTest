{
  "name": "qolony-example",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc && cp ./package.json ./dist/package.json",
    "start": "yarn build && sam build && sam local start-api",
    "deploy": "sam deploy"
  },
  "engines": {
    "node": "12"
  },
  "devDependencies": {
    "ts-node": "^9.0.0"
  },
  "dependencies": {
    "@types/aws-serverless-express": "^3.3.3",
    "@types/continuation-local-storage": "^3.2.2",
    "@types/cuid": "^1.3.1",
    "@types/express": "^4.17.8",
    "aws-lambda": "^1.0.6",
    "aws-serverless-express": "^3.3.8",
    "body-parser": "^1.19.0",
    "continuation-local-storage": "^3.2.1",
    "cuid": "^2.1.8",
    "express": "^4.17.1",
    "typescript": "^4.0.3"
  }
}
